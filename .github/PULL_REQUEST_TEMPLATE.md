## Summary

<!-- What does this change, and why? -->

## Checklist

- [ ] `just check` passes (fmt + clippy + tests) — requires Rust + Zig 0.15.2 + Bun
- [ ] Conventional, lowercase commit subject(s); no unrelated changes
- [ ] Docs updated if user-facing behavior changed

<!-- Note: the public `dzevs/zynk` repo is a curated export of a private source-of-truth repo.
     Accepted changes are re-applied in private and re-exported, so a maintainer may guide larger
     contributions or land them on your behalf. -->
